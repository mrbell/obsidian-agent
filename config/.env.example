# Secrets for obsidian-agent
# Copy to ~/.config/obsidian-agent/.env and fill in values (chmod 600)

# SMTP password — referenced by delivery.email.password_env in config.yaml
OBSIDIAN_AGENT_SMTP_PASSWORD=
HF_TOKEN=